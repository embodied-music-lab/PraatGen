# PRAAT COMMANDS: Harmonicity
# Source: Praat C API (praatlib.h), June 2025
# Additional commands: Session-verified via Paste Commands (Feb 2026)
# Part of EML PraatGen GPL-3.0-or-later — Ian Howell, Embodied Music Lab

---

  # Query (session-verified)
  Get value at time: time, interpolation$
  # Verified: Get value at time: 0.5, "cubic"
  Get mean: from time, to time
  # Verified: Get mean: 0, 0

  # Modify
  Formula: formula$

  # Convert / Analyze
  To Matrix

  # Draw
  Draw: from time, to time, minimum, maximum
  # Verified: Draw: 0, 0, 0, 40
  # NOTE: 4 parameters only — NO garnish parameter. Differs from
  # Intensity Draw which takes 5 (including garnish$). Passing a 5th
  # argument causes: "requires only 4 arguments, not the 5 given"
  # To suppress default garnish, use manual axis commands (Rule 28H).
  # Provenance: Runtime error + fix, 22 April 2026.

---

*End of Harmonicity commands.*
