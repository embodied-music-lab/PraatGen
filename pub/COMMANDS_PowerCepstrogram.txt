# PRAAT COMMANDS: PowerCepstrogram
# Source: Praat C API (praatlib.h), June 2025
# Additional commands: Session-verified via Paste Commands (Feb 2026)
# Part of EML PraatGen GPL-3.0-or-later — Ian Howell, Embodied Music Lab
#
# Format: CommandName: param1, param2 (if arguments)
#         CommandName (no colon, if no arguments)
# Parameter notation: $ = string, ? = boolean, others = numeric
#
# Cross-type dependencies: None

---

  # Query (session-verified)
  Get CPPS: subtract tilt before smoothing$, time averaging window, quefrency averaging window, peak search pitch floor, peak search pitch ceiling, tolerance, interpolation$, tilt line quefrency floor, tilt line quefrency ceiling, line type$, fit method$
# Verified: Get CPPS: "no", 0.01, 0.001, 60, 330, 0.05, "parabolic", 0.001, 0, "Straight", "Robust"
  # NOTE: Parameters per Maryn & Weenink (2015) / Phonanium Cepstrography v01.02.
  # Use these values unless user specifies otherwise.
  # ERRATUM (21 Apr 2026): Prior version had Praat dialog defaults here.

  # Convert / Analyze
  To PowerCepstrum (slice): time
  To Matrix

  # Draw
  Paint: from time, to time, from quefrency, to quefrency, maximum d b, autoscaling?, dynamic range d b, compression, garnish?

  # Other
  Subtract trend (in-place): from quefrency trend line, to quefrency trend line, line type$, fit method$
  Smooth: smoothing window duration, quefrency smoothing window duration
  Subtract trend: from quefrency trend line, to quefrency trend line, line type$, fit method$
# ============================================================================
# PowerCepstrum (distinct object type)
# Created from PowerCepstrogram via: To PowerCepstrum (slice): time
# Session-verified via Paste Commands (Feb 2026)
# ============================================================================
#
# NOTE: PowerCepstrum stores raw power values internally.
# Draw: and Draw trend line: convert to dB for display.
# If querying bounds via To Matrix → Get minimum/Get maximum,
# the returned values are RAW POWER, not dB. You must convert
# manually or use Get peak: which returns dB directly.

  # Query (session-verified)
  Get peak: pitch floor, pitch ceiling, interpolation$
  # Verified: Get peak: 60, 333.3, "parabolic"
  # Returns: peak dB value (already in dB scale)
  # Provenance: Paste Commands 25 Feb 2026 (A2 v2.6 cepstrum simplification)

  Get quefrency of peak: pitch floor, pitch ceiling, interpolation$
  # Verified: Get quefrency of peak: 60, 333.3, "parabolic"
  # Returns: quefrency in seconds.
  # Provenance: Praat GUI screenshot, 6 Mar 2026

  Get peak prominence: pitch floor, pitch ceiling, interpolation$, tilt line quefrency floor, tilt line quefrency ceiling, line type$, fit method$
  # Verified: Get peak prominence: 60, 330, "parabolic", 0.001, 0.05, "Straight", "Robust"
  # Returns: CPP value in dB
  # Provenance: A2 batch analyzer v2.0; Paste Commands 25 Feb 2026

  # Draw
  Draw: from quefrency, to quefrency, from power dB, to power dB, garnish?
  # Verified: Draw: 0, 0.05, 0, 120, "no"
  # NOTE: Parameters 3-4 are dB units. Use 0, 0 for auto-scale.
  # Provenance: Paste Commands 25 Feb 2026

  Draw trend line: from quefrency, to quefrency, from power dB, to power dB, tilt line quefrency floor, tilt line quefrency ceiling, line type$, fit method$
  # Verified: Draw trend line: 0, 0.05, 0, 120, 0.001, 0.05, "Straight", "Robust"
  # NOTE: Parameters 3-4 are dB units. Use 0, 0 for auto-scale.
  # Provenance: Paste Commands 25 Feb 2026

  # Convert
  To Matrix
  # NOTE: Matrix values are RAW POWER, not dB. See note at top of section.

  # Other
  Subtract trend: from quefrency, to quefrency, line type$, fit method$

# ============================================================================
  # Per-frame CPPS workflow (no slice loop needed)
  # ============================================================================
  # PowerCepstrogram → Subtract trend (in-place) → Smooth → To Table
  # The resulting Table contains per-frame smoothed CPP values.
  # Eliminates the need to loop through To PowerCepstrum (slice): per frame.
  # Column names: [NEED PASTE COMMANDS VERIFICATION]
  # Provenance: Natalie Choir extraction pipeline, 15 April 2026.


# ----------------------------------------------------------------------------


---

*End of PowerCepstrogram commands.*
